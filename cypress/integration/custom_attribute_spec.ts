import { CoreConfig } from '../../support/commands';

describe('Setting a custom attribute for an XXXX', function () {
  before('Load all configs', () => {
  });

  beforeEach(() => {
    // login with the admin user
  });

  it('The Admin user setup a custom attribute for an XXXX', function () {
  });

  it('The Custom Attribute is now showing in the XXXX details page', function () {
  });

  after('Remove the assigned custom attribute for an XXXX', () => {
  });
});
